package cmdHandler

/*
Sessions subcommand: list auth sessions from the login_session store.
With --check, probe Cursor once per session and print
valid / invalid / error (still one-shot CLI, not tied to serve).
*/
